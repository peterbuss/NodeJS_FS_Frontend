<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title><%=title%></title>
        <link rel="icon" type="image/x-icon" href="../favicon.ico" />
        <!-- Font Awesome icons (free version)-->
        <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
        <!-- Google fonts-->
        <link href="https://fonts.googleapis.com/css?family=Varela+Round" rel="stylesheet" />
        <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet" />
        <!-- Core theme CSS (includes Bootstrap)-->
        <link href="../css/styles.css" rel="stylesheet" type="text/css" />
    </head>
    <body id="page-top">
        <!-- Navigation-->
        <%-include('includes/nav')%>
 
        <!-- Signup-->
        <section class="signup-section" id="signup">
            <div class="container px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5">
                    <div class="col-md-10 col-lg-8 mx-auto">
                        <i class="far fa-paper-plane fa-2x mb-2 text-white"></i>
                        <h2 class="text-white mb-5">Add a book!</h2>
              
                        <form class="form-signup" id="addBookForm"
                            action="/books"
                            method="POST"                        
                        >
                        <% if(typeof(message) !=  "undefined") {%>
                        <h4 style="color: green"><%- message %></h4> 
                        <%}%>
                            <!-- First Name-->
                            <div class="row input-group-newsletter">
                                <div class="col">
                                    <input 
                                        class="form-control" 
                                        id="title" 
                                        type="text" 
                                        name="title"
                                        placeholder="Enter title..." aria-label="Enter title..." 
                                            <% if(typeof(body) !== "undefined") { %>
                                                value="<%-body.title%>" <%}%> 
                                    />
                                    <% if(typeof(errs) !=  "undefined") {%>
                                        <span style="color: red"><%-errs.titleMsg%></span> 
                                    <% } %>
    
                                </div>

                            </div>
                            <div class="space"></div>
                            <!-- Last Name input-->
                            <div class="row input-group-newsletter">
                                <div class="col">
                                    <input 
                                        class="form-control" 
                                        id="author" 
                                        type="text" 
                                        name="author"
                                        placeholder="Enter author..." 
                                        aria-label="Enter author..." 
                                        <% if(typeof(body) !== "undefined") { %>
                                            value="<%-body.author%>" <%}%>                                         
                                    />
                                </div>
                                <% if(typeof(errs) !=  "undefined") {%>
                                    <span style="color: red"><%-errs.authorMsg%></span> 
                                <% } %>                                
                            </div>
                            <div class="space"></div>
                            <!-- Email address input-->
                            <div class="row input-group-newsletter">
                                <div class="col">
                                    <input 
                                        class="form-control" 
                                        id="ISBN" 
                                        type="text" 
                                        name="ISBN"
                                        placeholder="Enter ISBN..." aria-label="Enter ISBN..." 
                                        <% if(typeof(body) !== "undefined") { %>
                                            value="<%-body.ISBN%>" <%}%>                                         
                                    />
                                </div>
                                <% if(typeof(errs) !=  "undefined") {%>
                                    <span style="color: red"><%-errs.ISBNMsg%></span> 
                                <% } %>
                            </div>
                            <div class="space"></div>

                            <!-- address -->
                            <div class="row input-group-newsletter space">
                                <div class="col">
                                    <input 
                                        class="form-control" 
                                        id="numberOfPages" 
                                        type="text" 
                                        name="numberOfPages"
                                        placeholder="Enter number of Pages..." 
                                        aria-label="Enter number of Pages..." 
                                        <% if(typeof(body) !== "undefined") { %>
                                            value="<%-body.numberOfPages%>" <%}%>                                         
                                    />
                                </div>
                                <% if(typeof(errs) !=  "undefined") {%>
                                    <span style="color: red"><%-errs.numberOfPagesMsg%></span> 
                                <% } %>
                            </div>

                            <!-- city -->
                            <div class="row input-group-newsletter space">
                                <div class="col">
                                    <input 
                                        class="form-control" 
                                        id="price" 
                                        type="text" 
                                        name="price"
                                        placeholder="Enter price.." 
                                        aria-label="Enter price..." 
                                        <% if(typeof(body) !== "undefined") { %>
                                            value="<%-body.price%>" <%}%>                                         
                                    />
                                </div>
                                <% if(typeof(errs) !=  "undefined") {%>
                                    <span style="color: red"><%-errs.priceMsg%></span> 
                                <% } %>
                            </div>
                            <!-- state-->
                            <div class="row input-group-newsletter space">
                                <div class="col">
                                    <input 
                                        class="form-control" 
                                        id="yearPublished" 
                                        type="text" 
                                        name="yearPublished"
                                        placeholder="Enter year Published..." 
                                        aria-label="Enter year Published..." 
                                        <% if(typeof(body) !== "undefined") { %>
                                            value="<%-body.yearPublished%>" <%}%>                                         
                                    />
                                </div>
                                <% if(typeof(errs) !=  "undefined") {%>
                                    <span style="color: red"><%-errs.yearPublishedMsg%></span> 
                                <% } %>
                            </div>
                            <div>
                                <div class="col-auto">
                                    <button 
                                        class="btn btn-primary" 
                                        id="submitButton"     
                                        type="submit">Add Book
                                    </button>
                                </div>
                            </div>

                        </form>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Footer-->
        <%-include('includes/footer.ejs')%>
        <!-- Bootstrap core JS-->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
        <!-- Core theme JS-->
        <script src="js/scripts.js"></script>
        <!-- * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *-->
        <!-- * *                               SB Forms JS                               * *-->
        <!-- * * Activate your form at https://startbootstrap.com/solution/contact-forms * *-->
        <!-- * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *-->
        <script src="https://cdn.startbootstrap.com/sb-forms-latest.js"></script>
    </body>
</html>
